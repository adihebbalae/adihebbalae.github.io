'use client';

import ProjectPage from '@/components/ProjectPage';
import attacca from '@/data/projects/attacca';

export default function AttaccaPage() {
  return <ProjectPage project={attacca} />;
}
