'use client';

import ProjectPage from '@/components/ProjectPage';
import glassbox from '@/data/projects/glassbox';

export default function GlassboxProjectPage() {
  return <ProjectPage project={glassbox} />;
}
