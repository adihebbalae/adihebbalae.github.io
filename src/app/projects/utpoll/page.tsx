'use client';

import ProjectPage from '@/components/ProjectPage';
import utpoll from '@/data/projects/utpoll';

export default function UtpollProjectPage() {
  return <ProjectPage project={utpoll} />;
}
